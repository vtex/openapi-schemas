info:
  name: Search for SKU
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/catalog-seller-portal/skus/_search"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  params:
    - name: from
      value: "1"
      type: query
      disabled: true
    - name: to
      value: "50"
      type: query
      disabled: true
    - name: id
      value: "1"
      type: query
      disabled: true
    - name: externalid
      value: "123456789"
      type: query
      disabled: true
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const Ajv = require("ajv");
        const addFormats = require("ajv-formats");
        const ajv = new Ajv({ allErrors: true, strict: false });
        addFormats(ajv);

        test("[GET]::/api/catalog-seller-portal/skus/_search - Status code is 2xx", function () {
          expect(res.getStatus()).to.be.at.least(200);
          expect(res.getStatus()).to.be.below(300);
        });

        test("[GET]::/api/catalog-seller-portal/skus/_search - Content-Type is application/json", function () {
          expect(String(res.getHeader("content-type"))).to.include("application/json");
        });

        test("[GET]::/api/catalog-seller-portal/skus/_search - Response has JSON Body", function () {
          const data = res.getBody();
          expect(data).to.not.be.undefined;
          expect(data).to.not.be.null;
        });

        test("[GET]::/api/catalog-seller-portal/skus/_search - Schema is valid", function () {
          const schema = {"type":"object","properties":{"data":{"type":"array","description":"List with information about the SKUs.","items":{"type":"object","description":"SKU information","properties":{"id":{"type":"string","description":"SKU unique identifier number."},"productId":{"type":"string","description":"Product unique identifier number."},"externalId":{"type":"string","description":"Unique identifier number of the association of the SKU with a Franchise Account."}}}},"_metadata":{"type":"object","description":"Information about the organization and exhibition of the SKU list.","properties":{"total":{"type":"integer","description":"Total of SKUs on the list."},"from":{"type":"integer","description":"The first page of the interval of the SKU list."},"to":{"type":"integer","description":"The last page of the interval of the SKU list."}}}}};
          const validate = ajv.compile(schema);
          const valid = validate(res.getBody());
          expect(valid, JSON.stringify(validate.errors)).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  >📘 This API is part of the [Seller Portal Catalog](https://help.vtex.com/en/tutorial/how-the-seller-portal-catalog-works--7pMB6YOt6YQDQQbzFB4Pxp). This functionality is in the Beta stage and can be discontinued at any moment at VTEX's discretion. VTEX will not be responsible for any instabilities caused by its use or discontinuity. If you have any questions, please contact [our Support Center](https://support.vtex.com/hc/en-us/requests). 

   Retrieves general information about an SKU taking into consideration the defined search criteria. It is mandatory to use at least one query parameter.

  ## Permissions

  Any user or [API key](https://developers.vtex.com/docs/guides/api-authentication-using-api-keys) must have at least one of the appropriate [License Manager resources](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3) to be able to successfully run this request. Otherwise they will receive a status code `403` error. These are the applicable resources for this endpoint:

  | **Product** | **Category** | **Resource** |
  | --------------- | ----------------- | ----------------- |
  | CatalogV2 | Management | **Product Read** |

  There are no applicable [predefined roles](https://help.vtex.com/en/tutorial/predefined-roles--jGDurZKJHvHJS13LnO7Dy) for this resource list. You must [create a custom role](https://help.vtex.com/en/tutorial/roles--7HKK5Uau2H6wxE1rH5oRbc#creating-a-role) and add at least one of the resources above in order to use this endpoint.To learn more about machine authentication at VTEX, see [Authentication overview](https://developers.vtex.com/docs/guides/authentication).

  >❗ To prevent integrations from having excessive permissions, consider the [best practices for managing API keys](https://help.vtex.com/en/tutorial/best-practices-api-keys--7b6nD1VMHa49aI5brlOvJm) when assigning License Manager roles to integrations.

  ## Parameters

  | Name | In | Description |
  | --- | --- | --- |
  | `from` | query | The first page of the interval of the product list. |
  | `to` | query | The last page of the interval of the product list. |
  | `id` | query | SKU unique idenfier number. |
  | `externalid` | query | SKU reference unique identifier number in the store. |
  | `Content-Type` | header | (Required) Type of the content being sent. |
  | `Accept` | header | (Required) HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand. |

  ## Example response (`200`)

  ```json
  {
    "data": [
      {
        "id": "2",
        "productId": "2",
        "externalId": "1909621862"
      }
    ],
    "_metadata": {
      "total": 1,
      "from": 1,
      "to": 15
    }
  }
  ```
