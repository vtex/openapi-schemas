info:
  name: Create recipient
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/dataentities/contact_information/documents"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "rafael.silva@example.com",
        "firstName": "Rafael",
        "lastName": "Silva",
        "profileId": "878fa807-7c7b-4ad3-aaa6-e3ef4fb22b8f",
        "phone": "+1 212 555 1234",
        "phoneExtension": "123",
        "document": "112-3456789",
        "documentType": "EIN",
        "addressIds": [
          "1ded2836-2061-4545-96d3-58f5338627c5",
          "a3e103fc-d074-44fb-941a-949f1bca937c"
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const Ajv = require("ajv");
        const addFormats = require("ajv-formats");
        const ajv = new Ajv({ allErrors: true, strict: false });
        addFormats(ajv);

        test("[POST]::/api/dataentities/contact_information/documents - Status code is 2xx", function () {
          expect(res.getStatus()).to.be.at.least(200);
          expect(res.getStatus()).to.be.below(300);
        });

        test("[POST]::/api/dataentities/contact_information/documents - Content-Type is application/json", function () {
          expect(String(res.getHeader("content-type"))).to.include("application/json");
        });

        test("[POST]::/api/dataentities/contact_information/documents - Response has JSON Body", function () {
          const data = res.getBody();
          expect(data).to.not.be.undefined;
          expect(data).to.not.be.null;
        });

        test("[POST]::/api/dataentities/contact_information/documents - Schema is valid", function () {
          const schema = {"type":"object","description":"Recipient document creation response.","properties":{"Id":{"type":"string","description":"Recipient document identifier. The prefix corresponds to the `contact_information` data entity name."},"Href":{"type":"string","description":"Recipient document store path."},"DocumentId":{"type":"string","description":"Recipient document ID."}}};
          const validate = ajv.compile(schema);
          const valid = validate(res.getBody());
          expect(valid, JSON.stringify(validate.errors)).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Creates a new recipient document in the `contact_information` data entity. A recipient represents a person who can receive orders at one or more B2B addresses. Use the `addressIds` field to associate the recipient with existing B2B addresses from the `AD` data entity.

  >⚠️ This feature is only available for stores using B2B Buyer Portal, which is currently available to select accounts.

  ## Permissions

  Any user or [API key](https://developers.vtex.com/docs/guides/api-authentication-using-api-keys) must have at least one of the appropriate [License Manager resources](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3) to be able to successfully run this request. Otherwise they will receive a status code `403` error. These are the applicable resources for this endpoint:

  | **Product** | **Category** | **Resource** |
  | --------------- | ----------------- | ----------------- |
  | Dynamic Storage | Dynamic storage generic resources | **Insert or update document (not remove)** |
  | Dynamic Storage | Dynamic storage generic resources | **Full access to all documents** |
  | Dynamic Storage | Dynamic storage generic resources | **Master Data administrator** |

  There are no applicable [predefined roles](https://help.vtex.com/en/tutorial/predefined-roles--jGDurZKJHvHJS13LnO7Dy) for this resource list. You must [create a custom role](https://help.vtex.com/en/tutorial/roles--7HKK5Uau2H6wxE1rH5oRbc#creating-a-role) and add at least one of the resources above in order to use this endpoint. To learn more about machine authentication at VTEX, see [Authentication overview](https://developers.vtex.com/docs/guides/authentication).

  >❗ To prevent integrations from having excessive permissions, consider the [best practices for managing API keys](https://help.vtex.com/en/tutorial/best-practices-api-keys--7b6nD1VMHa49aI5brlOvJm) when assigning License Manager roles to integrations.

  ## Parameters

  | Name | In | Description |
  | --- | --- | --- |
  | `Content-Type` | header | (Required) Type of the content being sent. |
  | `Accept` | header | (Required) HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand. |

  ## Example response (`201`)

  ```json
  {
    "Id": "contact_information-123e4567-e89b-12d3-a456-426614174000",
    "Href": "http://{{account}}.myvtex.com/api/dataentities/contact_information/documents/123e4567-e89b-12d3-a456-426614174000?_schema=v1",
    "DocumentId": "123e4567-e89b-12d3-a456-426614174000"
  }
  ```
