info:
  name: Edit storefront role resources
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/license-manager/storefront/role/:roleId?an=apiexamples"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  params:
    - name: an
      value: apiexamples
      type: query
    - name: roleId
      value: "42"
      type: path
  body:
    type: json
    data: |-
      {
        "Resources": [
          1,
          9
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const Ajv = require("ajv");
        const addFormats = require("ajv-formats");
        const ajv = new Ajv({ allErrors: true, strict: false });
        addFormats(ajv);

        test("[PUT]::/api/license-manager/storefront/role/:roleId - Status code is 2xx", function () {
          expect(res.getStatus()).to.be.at.least(200);
          expect(res.getStatus()).to.be.below(300);
        });

        test("[PUT]::/api/license-manager/storefront/role/:roleId - Content-Type is application/json", function () {
          expect(String(res.getHeader("content-type"))).to.include("application/json");
        });

        test("[PUT]::/api/license-manager/storefront/role/:roleId - Response has JSON Body", function () {
          const data = res.getBody();
          expect(data).to.not.be.undefined;
          expect(data).to.not.be.null;
        });

        test("[PUT]::/api/license-manager/storefront/role/:roleId - Schema is valid", function () {
          const schema = {"type":"object","description":"Updated role.","properties":{"Id":{"type":"integer","description":"Role identifier."},"Name":{"type":"string","description":"Role name."},"Resources":{"type":"array","description":"Updated list of resources assigned to the role.","items":{"type":"object","description":"Resource information.","properties":{"Id":{"type":"integer","description":"Resource identifier."},"Key":{"type":"string","description":"Stable permission key."},"IsCustom":{"type":"boolean","description":"Set to `true` when custom or `false` for native."}}}}}};
          const validate = ajv.compile(schema);
          const valid = validate(res.getBody());
          expect(valid, JSON.stringify(validate.errors)).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Replaces the full resource list of an existing custom role. Send all resources you want the role to have after the update.

  > ⚠️ This feature is available only for stores using B2B Buyer Portal, currently available for select accounts.

  ## Permissions

  Any user or [API key](https://developers.vtex.com/docs/guides/authentication-overview#api-keys) must have at least one of the appropriate [License Manager resources](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3) to be able to successfully run this request. Otherwise, they will receive a status code `403` error. These are the applicable resources for this endpoint:

  | **Product** | **Category** | **Resource** |
  | --------------- | ----------------- | ----------------- |
  | License Manager | Services access control | **Edit Storefront User Permissions** |

  There are no applicable [predefined roles](https://help.vtex.com/en/tutorial/predefined-roles--jGDurZKJHvHJS13LnO7Dy) for this resource list. You must [create a custom role](https://help.vtex.com/en/tutorial/roles--7HKK5Uau2H6wxE1rH5oRbc#creating-a-role) and add at least one of the resources above in order to use this endpoint.

  To learn more about machine authentication at VTEX, see [Authentication overview](https://developers.vtex.com/docs/guides/authentication-overview#machine-authentication).

  >❗ To prevent integrations from having excessive permissions, consider the [best practices for managing API keys](https://help.vtex.com/en/tutorial/best-practices-api-keys--7b6nD1VMHa49aI5brlOvJm) when assigning License Manager roles to integrations.

  ## Parameters

  | Name | In | Description |
  | --- | --- | --- |
  | `roleId` | path | (Required) Numeric identifier of the role to update. |
  | `an` | query | (Required) VTEX account name. |
  | `Content-Type` | header | (Required) Type of the content being sent. |
  | `Accept` | header | (Required) HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand. |

  ## Example response (`200`)

  ```json
  {
    "Id": 42,
    "Name": "Report Viewer",
    "Resources": [
      {
        "Id": 1,
        "Key": "ManageOrganizationAndContract",
        "IsCustom": false
      },
      {
        "Id": 9,
        "Key": "AnotherResource",
        "IsCustom": true
      }
    ]
  }
  ```
