info:
  name: Create SKU attribute
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/catalog/pvt/stockkeepingunit/:skuId/attribute"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  params:
    - name: skuId
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "SpecificationName": "Ingredients",
        "SpecificationValue": "wax, oil, pigment",
        "IsVisible": true
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const Ajv = require("ajv");
        const addFormats = require("ajv-formats");
        const ajv = new Ajv({ allErrors: true, strict: false });
        addFormats(ajv);

        test("[POST]::/api/catalog/pvt/stockkeepingunit/:skuId/attribute - Status code is 2xx", function () {
          expect(res.getStatus()).to.be.at.least(200);
          expect(res.getStatus()).to.be.below(300);
        });

        test("[POST]::/api/catalog/pvt/stockkeepingunit/:skuId/attribute - Content-Type is application/json", function () {
          expect(String(res.getHeader("content-type"))).to.include("application/json");
        });

        test("[POST]::/api/catalog/pvt/stockkeepingunit/:skuId/attribute - Response has JSON Body", function () {
          const data = res.getBody();
          expect(data).to.not.be.undefined;
          expect(data).to.not.be.null;
        });

        test("[POST]::/api/catalog/pvt/stockkeepingunit/:skuId/attribute - Schema is valid", function () {
          const schema = {"type":"object","properties":{"Id":{"type":"integer","description":"SKU attribute unique numerical identifier."},"SkuId":{"type":"integer","description":"SKU's unique numerical identifier."},"SpecificationName":{"type":"string","description":"Name of the SKU attribute."},"SpecificationValue":{"type":"string","maxLength":3500,"description":"Value of the SKU attribute."},"IsVisible":{"type":"boolean","description":"Defines if the attribute is visible on the store (`true`) or not (`false`)."}}};
          const validate = ajv.compile(schema);
          const valid = validate(res.getBody());
          expect(valid, JSON.stringify(validate.errors)).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Creates an attribute for an SKU, which is an informative field at the SKU level that doesn't create new variants. For example, a lipstick might have color and size as structured specifications that define SKU variants, while ingredients would be an informative attribute since they don't represent a new SKU.

  >⚠️ Attributes only accept open-text values. The maximum length for `SpecificationValue` is 3,500 characters. If the request body contains characters that break the JSON string (for example, unescaped backslashes), the request will fail with a `400` status code.

  You can also create and edit attributes via VTEX Admin, in **Catalog > Products and SKUs > [SKU editing page](https://help.vtex.com/docs/tutorials/adding-or-editing-skus)** in the **Attributes** section.

  ## Permissions

  Any user or [API key](https://developers.vtex.com/docs/guides/api-authentication-using-api-keys) must have at least one of the appropriate [License Manager resources](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3) to be able to successfully run this request. Otherwise they will receive a status code `403` error. These are the applicable resources for this endpoint:

  | **Product** | **Category** | **Resource** |
  | --------------- | ----------------- | ----------------- |
  | Catalog API | General | **Edit Product** |

  There are no applicable [predefined roles](https://help.vtex.com/en/tutorial/predefined-roles--jGDurZKJHvHJS13LnO7Dy) for this resource list. You must [create a custom role](https://help.vtex.com/en/tutorial/roles--7HKK5Uau2H6wxE1rH5oRbc#creating-a-role) and add at least one of the resources above in order to use this endpoint. To learn more about machine authentication at VTEX, see [Authentication overview](https://developers.vtex.com/docs/guides/authentication).

  >❗ To prevent integrations from having excessive permissions, consider the [best practices for managing API keys](https://help.vtex.com/en/tutorial/best-practices-api-keys--7b6nD1VMHa49aI5brlOvJm) when assigning License Manager roles to integrations.

  ## Parameters

  | Name | In | Description |
  | --- | --- | --- |
  | `skuId` | path | (Required) SKU's unique numerical identifier. |
  | `Content-Type` | header | (Required) Type of the content being sent. |
  | `Accept` | header | (Required) HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand. |

  ## Example response (`200`)

  ```json
  {
    "Id": 1171,
    "SkuId": 1,
    "SpecificationName": "Ingredients",
    "SpecificationValue": "wax, oil, pigment",
    "IsVisible": true
  }
  ```
