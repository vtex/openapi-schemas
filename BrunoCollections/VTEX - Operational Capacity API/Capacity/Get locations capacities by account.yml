info:
  name: Get locations capacities by account
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseUrl}}/api/fulfillment-locations/capacity/by-parent-account-name"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const Ajv = require("ajv");
        const addFormats = require("ajv-formats");
        const ajv = new Ajv({ allErrors: true, strict: false });
        addFormats(ajv);

        test("[PUT]::/api/fulfillment-locations/capacity/by-parent-account-name - Status code is 2xx", function () {
          expect(res.getStatus()).to.be.at.least(200);
          expect(res.getStatus()).to.be.below(300);
        });

        test("[PUT]::/api/fulfillment-locations/capacity/by-parent-account-name - Content-Type is application/json", function () {
          expect(String(res.getHeader("content-type"))).to.include("application/json");
        });

        test("[PUT]::/api/fulfillment-locations/capacity/by-parent-account-name - Response has JSON Body", function () {
          const data = res.getBody();
          expect(data).to.not.be.undefined;
          expect(data).to.not.be.null;
        });

        test("[PUT]::/api/fulfillment-locations/capacity/by-parent-account-name - Schema is valid", function () {
          const schema = {"type":"array","items":{"type":"object","description":"Object that represents the capacity information for a fulfillment location, including its identifier, account details, and availability window.","properties":{"locationId":{"type":"string","description":"Unique identifier for the location."},"accountName":{"type":"string","description":"Name of the account associated with the location."},"accountId":{"type":"string","description":"Identifier for the VTEX account."},"availability":{"type":"object","description":"Availability details including usage percentage and time window.","properties":{"amountUsagePercentage":{"type":"number","description":"Percentage of capacity currently in use."},"startAt":{"type":"string","format":"date-time","description":"Start time of the availability period."},"endAt":{"type":"string","format":"date-time","description":"End time of the availability period."}}}}}};
          const validate = ajv.compile(schema);
          const valid = validate(res.getBody());
          expect(valid, JSON.stringify(validate.errors)).to.be.true;
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Retrieves the current capacities of all locations associated with a parent account.

  ## Permissions

  Any user or [API key](https://developers.vtex.com/docs/guides/api-authentication-using-api-keys) must have at least one of the appropriate [License Manager resources](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3) to be able to successfully run this request. Otherwise they will receive a status code `403` error. These are the applicable resources for this endpoint:

  | **Product** | **Category** | **Resource** |
  | --------------- | ----------------- | ----------------- |
  | Logistics | Logistics access | **Logistics full access** |

  There are no applicable [predefined roles](https://help.vtex.com/en/tutorial/predefined-roles--jGDurZKJHvHJS13LnO7Dy) for this resource list. You must [create a custom role](https://help.vtex.com/en/tutorial/roles--7HKK5Uau2H6wxE1rH5oRbc#creating-a-role) and add at least one of the resources above in order to use this endpoint. To learn more about machine authentication at VTEX, see [Authentication overview](https://developers.vtex.com/docs/guides/authentication).

  >❗ To prevent integrations from having excessive permissions, consider the [best practices for managing API keys](https://help.vtex.com/en/tutorial/best-practices-api-keys--7b6nD1VMHa49aI5brlOvJm) when assigning License Manager roles to integrations.

  ## Parameters

  | Name | In | Description |
  | --- | --- | --- |
  | `Accept` | header | (Required) HTTP Client Negotiation _Accept_ Header. Indicates the types of responses the client can understand. |
  | `Content-Type` | header | (Required) Type of the content being sent. |

  ## Example response (`200`)

  ```json
  [
    {
      "locationId": "locationId_3",
      "accountName": "seller_2",
      "accountId": "id_2",
      "availability": {
        "amountUsagePercentage": 0,
        "startAt": "2025-02-10T00:00:00-03:00",
        "endAt": "2025-02-10T23:59:59-03:00"
      }
    }
  ]
  ```
